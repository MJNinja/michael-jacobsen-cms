<?php if($moduleID == 5){ ?>
	<script type="text/javascript" language="javascript">
        /*REMOVE WEBSITE*/
        function deleteWebsite(bos) {
            var msg = "Are you sure you want to remove the selected Website? You can always recover the website at a later stage.";
            if (confirm(msg)) {
                eval('document.delete_website'+bos+'.method = "post"');
                eval('document.delete_website'+bos+'.action = "<?php echo $_SERVER['PHP_SELF'] ?>"');
                eval('document.delete_website'+bos+'.submit()');
            } else {
                return;
            }
        }
	</script>

	<script type="text/javascript" language="javascript">
        /*RECOVER WEBSITE*/
        function recoverWebsite(bos) {
            var msg = "Are you sure you want to recover the selected Software?";
            if (confirm(msg)) {
                eval('document.recover_website'+bos+'.method = "post"');
                eval('document.recover_website'+bos+'.action = "<?php echo $_SERVER['PHP_SELF'] ?>"');
                eval('document.recover_website'+bos+'.submit()');
            } else {
                return;
            }
        }
    </script>

	<script type="text/javascript" language="javascript">
        /*REMOVE PARAGRAPH*/
        function deleteParagraph(bos) {
            var msg = "Are you sure you want to remove the selected Paragraph? Once it is removed, it can't be recovered.";
            if (confirm(msg)) {
                eval('document.delete_paragraph'+bos+'.method = "post"');
                eval('document.delete_paragraph'+bos+'.action = "<?php echo $_SERVER['PHP_SELF'] ?>"');
                eval('document.delete_paragraph'+bos+'.submit()');
            } else {
                return;
            }
        }
	</script>

	<script type="text/javascript" language="javascript">
        /*REMOVE GALLERY*/
        function deleteGallery(bos) {
            var msg = "Are you sure you want to remove the selected Gallery? Once it is removed, it can't be recovered.";
            if (confirm(msg)) {
                eval('document.delete_gallery'+bos+'.method = "post"');
                eval('document.delete_gallery'+bos+'.action = "<?php echo $_SERVER['PHP_SELF'] ?>"');
                eval('document.delete_gallery'+bos+'.submit()');
            } else {
                return;
            }
        }
	</script>
<?php } ?>